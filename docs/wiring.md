# Wiring

Record the XIAO ESP32-C6 pin map, Grove ports, power paths, and shared grounds here.

Do not assume a Grove module is I2C just because it uses a Grove cable; verify the exact module type first.
